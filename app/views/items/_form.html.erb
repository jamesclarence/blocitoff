// Form partial for users to submit new items
// Should render the form for submitting a new item
// Renders a single item, therefore you need to call it multiple
	// times, rendering a partial for each item in @items.