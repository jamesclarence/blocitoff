<div class="col-md-6">
<% @items.each do |i| %>
	<div class="row">
		<%= i.body %>
		<%= link_to "", item_path(i.id), method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
	</div>
<% end %>

</div>
